(()=>{
'use strict';
if(window.BOS_MASTER_RESCHEDULE_CALL_V87)return;window.BOS_MASTER_RESCHEDULE_CALL_V87=true;
const META_URL='https://obsropbslfwtanyspjbi.supabase.co/functions/v1/order-meta-api';
const getOrder=id=>(state?.orders||[]).find(x=>String(x.id)===String(id));
const storedSession=()=>{try{return sessionStorage.getItem('bos_vk_session_v2')||localStorage.getItem('bos_vk_session_v2')||''}catch(_){return ''}};
const clearSession=()=>{try{sessionStorage.removeItem('bos_vk_session_v2');localStorage.removeItem('bos_vk_session_v2')}catch(_){}};
async function metaHeaders(){let token=storedSession();if(!token&&window.BOS_ENSURE_VK_SESSION){try{await window.BOS_ENSURE_VK_SESSION();token=storedSession()}catch(_){}}if(!token)throw new Error('Доступ не подтверждён');return{'Content-Type':'application/json','X-BOS-Session':token}}
async function metaRequest(body){let lastErr;for(let attempt=0;attempt<2;attempt++){try{const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),18000);try{const r=await fetch(META_URL,{method:'POST',headers:await metaHeaders(),body:JSON.stringify(body),signal:controller.signal});const d=await r.json().catch(()=>({}));if(r.status===401&&attempt===0){clearSession();if(window.BOS_ENSURE_VK_SESSION){try{await window.BOS_ENSURE_VK_SESSION()}catch(_){}}lastErr=new Error(d.error||'Доступ не подтверждён');continue}if(!r.ok||!d.ok){const e=new Error(d.error||`Ошибка сервера (${r.status})`);e.status=r.status;throw e}return d}finally{clearTimeout(timer)}}catch(e){lastErr=e;if(e?.name==='AbortError')lastErr=new Error('Сервер не ответил. Повторите ещё раз.');if(attempt===0)await new Promise(r=>setTimeout(r,350))}}throw lastErr||new Error('Не удалось отправить запрос')}
function actingMaster(body){if(typeof isMasterPreview==='function'&&isMasterPreview()&&typeof liveMasterUser==='function'){const m=liveMasterUser();body.acting_master_vk_id=m?.vk_user_id||m?.external_id||m?.id||''}return body}
function telValue(raw){const source=String(raw||'').trim(),digits=source.replace(/\D/g,'');if(!digits)return'';if(digits.length===11&&digits.startsWith('8'))return'+7'+digits.slice(1);if(digits.length===10)return'+7'+digits;if(source.startsWith('+'))return'+'+digits;return digits}
function modalRoot(){return document.querySelector('#modalRoot .modal')||document.querySelector('#modalRoot')}
function injectCall(id){const o=getOrder(id),phone=String(o?.phone||o?.client_phone||'').trim(),tel=telValue(phone),modal=modalRoot();if(!o||!phone||!tel||!modal||modal.querySelector('.bosClientCallAction'))return;let target=[...modal.querySelectorAll('small')].find(el=>(el.textContent||'').trim()===phone)?.parentElement;target=target||[...modal.querySelectorAll('.bosOrderFact')].find(el=>(el.textContent||'').includes(phone));target=target||[...modal.querySelectorAll('p,div')].find(el=>el.children.length<4&&(el.textContent||'').includes(phone));if(!target)return;const a=document.createElement('a');a.className='secondary bosClientCallAction';a.href=`tel:${tel}`;a.textContent='Позвонить';a.setAttribute('aria-label',`Позвонить клиенту ${phone}`);a.onclick=e=>e.stopPropagation();target.appendChild(a)}
function injectRescheduleNotice(id){const o=getOrder(id),modal=modalRoot();if(!o?.reschedule_requested||!modal||modal.querySelector('.bosRescheduleNotice'))return;const box=document.createElement('div');box.className='bosRescheduleNotice';const reason=String(o.reschedule_reason||'').trim();box.innerHTML=`<b>Нужно перенести</b>${reason?`<span>${esc(reason)}</span>`:''}`;const anchor=modal.querySelector('.bosOrderHead,.bosHandsHead,h2');if(anchor)anchor.insertAdjacentElement('afterend',box);else modal.prepend(box)}
function injectOrderExtras(id){injectCall(id);injectRescheduleNotice(id)}
const baseOpenOrder=window.openOrder;
if(typeof baseOpenOrder==='function')window.openOrder=function(id){const out=baseOpenOrder.apply(this,arguments);setTimeout(()=>injectOrderExtras(id),0);setTimeout(()=>injectOrderExtras(id),80);return out};
function injectReportAction(id){const form=document.querySelector('#masterReportForm');if(!form||form.querySelector('.bosRescheduleAction'))return;const btn=document.createElement('button');btn.type='button';btn.className='secondary wide bosRescheduleAction';btn.textContent='Нужно перенести';btn.onclick=()=>window.openMasterRescheduleForm(id);const submit=form.querySelector('button[type="submit"]');if(submit)submit.insertAdjacentElement('afterend',btn);else form.appendChild(btn);const o=getOrder(id);if(o?.reschedule_requested){const p=document.createElement('p');p.className='muted bosRescheduleExisting';p.textContent=o.reschedule_reason?`Запрос уже отправлен: ${o.reschedule_reason}`:'Запрос на перенос уже отправлен';btn.insertAdjacentElement('afterend',p)}}
const baseOpenReport=window.openMasterReportForm;
if(typeof baseOpenReport==='function')window.openMasterReportForm=function(id){const out=baseOpenReport.apply(this,arguments);setTimeout(()=>injectReportAction(id),0);setTimeout(()=>injectReportAction(id),50);return out};
window.openMasterRescheduleForm=function(id){const o=getOrder(id);if(!o)return;const current=String(o.reschedule_reason||'');openModal(`<h2>Нужно перенести заявку ${esc(o.id)}</h2><form id="masterRescheduleForm" class="form"><label for="masterRescheduleReason">Причина переноса *</label><textarea id="masterRescheduleReason" name="reason" maxlength="1000" rows="5" required placeholder="Напишите, почему заявку нужно перенести">${esc(current)}</textarea><button class="primary wide" type="submit">Отправить запрос на перенос</button><button class="secondary wide" type="button" onclick="openMasterReportForm('${esc(o.id)}')">Назад к отчёту</button><p id="masterRescheduleMsg" class="muted"></p></form>`);const form=document.querySelector('#masterRescheduleForm'),reason=document.querySelector('#masterRescheduleReason');setTimeout(()=>reason?.focus(),0);form.onsubmit=async e=>{e.preventDefault();if(state.busy)return;const value=String(reason?.value||'').trim(),msg=document.querySelector('#masterRescheduleMsg');if(value.length<3){msg.textContent='Укажите причину переноса';return}state.busy=true;setBusy(form,true);msg.textContent='Отправляем запрос…';try{const d=await metaRequest(actingMaster({action:'requestReschedule',id:o.id,reason:value}));const i=(state.orders||[]).findIndex(x=>String(x.id)===String(o.id));if(i>=0)state.orders[i]={...state.orders[i],...(d.order||{})};msg.textContent='Запрос на перенос отправлен';state.busy=false;closeModal();show('orders')}catch(err){msg.textContent=err?.message||String(err);setBusy(form,false);state.busy=false}}};
const style=document.createElement('style');style.textContent=`.bosClientCallAction{display:inline-flex!important;align-items:center;justify-content:center;width:max-content;min-height:32px;padding:6px 11px;margin:7px 0 0 8px;text-decoration:none;font-size:12px}.bosHandsBlock .bosClientCallAction{margin-left:0}.bosRescheduleAction{margin-top:8px!important}.bosRescheduleExisting{margin:4px 0 0;font-size:12px}.bosRescheduleNotice{display:grid;gap:3px;margin:8px 0 10px;padding:10px 12px;border:1px solid rgba(242,176,65,.38);border-radius:12px;background:rgba(242,176,65,.10)}.bosRescheduleNotice b{font-size:13px}.bosRescheduleNotice span{font-size:12px;line-height:1.35;overflow-wrap:anywhere}#masterRescheduleReason{min-height:110px;resize:vertical}@media(max-width:520px){.bosClientCallAction{margin-left:0;display:flex!important;width:100%;box-sizing:border-box}.bosRescheduleNotice{margin-top:6px}}`;document.head.appendChild(style);
})();
