(()=>{
  const forceVkAuth=new URLSearchParams(location.search).has('force_vk_auth');
  const local=/^(localhost|127\.0\.0\.1|0\.0\.0\.0)$/.test(location.hostname)&&!forceVkAuth;
  const SESSION_KEY='bos_vk_session_v2';
  window.BOS_LOCAL_DEV=local;window.BOS_VK_LAUNCH_PARAMS='';let launchPromise=null,tokenAuthPromise=null;
  function getSession(){try{return sessionStorage.getItem(SESSION_KEY)||localStorage.getItem(SESSION_KEY)||''}catch(_){return ''}}
  function setSession(v){if(!v)return;try{sessionStorage.setItem(SESSION_KEY,v);localStorage.setItem(SESSION_KEY,v)}catch(_){}}
  function clearSession(){try{sessionStorage.removeItem(SESSION_KEY);localStorage.removeItem(SESSION_KEY)}catch(_){}}
  function regError(msg='Требуется регистрация по номеру телефона'){const e=new Error(msg);e.code='REGISTRATION_REQUIRED';return e}
  function signed(raw){raw=String(raw||'').replace(/^\?/,'');const p=new URLSearchParams(raw);return p.has('vk_app_id')&&p.has('vk_user_id')&&p.has('sign')?raw:''}
  function fromObject(obj){if(!obj)return '';const src=obj.launch_params||obj;const p=new URLSearchParams();Object.keys(src).filter(k=>k==='sign'||k.startsWith('vk_')).sort().forEach(k=>{const v=src[k];if(v!==undefined&&v!==null)p.set(k,String(v))});return signed(p.toString())}
  function immediateLaunch(){let raw=signed(location.search);if(raw)return raw;const hash=String(location.hash||'');const q=hash.includes('?')?hash.slice(hash.indexOf('?')+1):hash.replace(/^#/,'');raw=signed(q);if(raw)return raw;try{const ref=new URL(document.referrer);raw=signed(ref.search);if(raw)return raw}catch(_){}return ''}
  async function bridgeLaunch(ms=6000){const started=Date.now();while(Date.now()-started<ms){if(window.vkBridge&&typeof window.vkBridge.send==='function'){try{await window.vkBridge.send('VKWebAppInit',{}).catch(()=>{});const d=await window.vkBridge.send('VKWebAppGetLaunchParams',{});const raw=fromObject(d);if(raw)return raw}catch(_){} }await new Promise(r=>setTimeout(r,180))}return ''}
  async function ensureLaunchParams(force=false){if(force)window.BOS_VK_LAUNCH_PARAMS='';if(window.BOS_VK_LAUNCH_PARAMS)return window.BOS_VK_LAUNCH_PARAMS;if(launchPromise)return launchPromise;launchPromise=(async()=>{let raw=immediateLaunch();if(!raw&&!local)raw=await bridgeLaunch(force?12000:6000);window.BOS_VK_LAUNCH_PARAMS=raw||'';return window.BOS_VK_LAUNCH_PARAMS})().finally(()=>{launchPromise=null});return launchPromise}
  window.BOS_ENSURE_VK_LAUNCH_PARAMS=ensureLaunchParams;
  window.BOS_STORE_SESSION=setSession;
  async function getVkAuthToken(){const app_id=Number(cfg.VK_APP_ID||54758847);const timeout=ms=>new Promise((_,reject)=>setTimeout(()=>reject(new Error('VK_AUTH_TIMEOUT')),ms));let lastErr=null;for(const scope of ['', 'friends']){try{const t=await Promise.race([window.vkBridge.send('VKWebAppGetAuthToken',{app_id,scope}),timeout(12000)]);if(t&&t.access_token)return t}catch(e){lastErr=e}}throw lastErr||new Error('VK_AUTH_FAILED')}
  async function tokenSession(){if(getSession())return getSession();if(local)return '';if(tokenAuthPromise)return tokenAuthPromise;tokenAuthPromise=(async()=>{if(!window.vkBridge||typeof window.vkBridge.send!=='function')throw new Error('VK Bridge недоступен');await window.vkBridge.send('VKWebAppInit',{}).catch(()=>{});let t=null;try{t=await getVkAuthToken()}catch(_){const launch=await ensureLaunchParams(true);if(launch)return '';throw new Error('Не удалось подтвердить аккаунт VK. Нажмите «Повторить» или переоткройте мини‑приложение во ВКонтакте.')}const access=String(t?.access_token||'');if(!access){const launch=await ensureLaunchParams(true);if(launch)return '';throw new Error('VK не выдал данные авторизации')}const r=await fetch('https://obsropbslfwtanyspjbi.supabase.co/functions/v1/vk-session-api',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({access_token:access})});const d=await r.json().catch(()=>({}));if(!r.ok||!d.ok){const launch=await ensureLaunchParams(true);if(launch)return '';throw new Error(d.error||'Не удалось подтвердить аккаунт VK')}if(!d.session_token)throw new Error('Сервер не создал сессию');setSession(d.session_token);if(d.registration_required)throw regError();return d.session_token})().finally(()=>{tokenAuthPromise=null});return tokenAuthPromise}
  window.BOS_ENSURE_VK_SESSION=tokenSession;
  window.BOS_AUTH_HEADERS=async function(){const h={'Content-Type':'application/json'},session=getSession();if(session){h['X-BOS-Session']=session}else{const launch=await ensureLaunchParams();if(launch)h['X-VK-Launch-Params']=launch}if(local)h['X-BOS-Local-Dev']='1';return h};
  async function requestApi(action,payload={},attempt=0){const url=cfg.API_URL||cfg.GAS_WEB_APP_URL;if(!url)throw new Error('Не настроен API');let session=getSession(),launch=session?'':await ensureLaunchParams();if(!session&&!launch&&!local)await tokenSession();const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),25000);const headers=await window.BOS_AUTH_HEADERS();try{const r=await fetch(url,{method:'POST',headers,body:JSON.stringify({action,...payload}),signal:controller.signal});let d;try{d=await r.json()}catch(_){throw new Error('Сервер вернул неверный ответ')}if(d?.session_token)setSession(d.session_token);if(d?.registration_required)throw regError(d.error||undefined);if(r.status===401&&attempt<2&&!local){clearSession();window.BOS_VK_LAUNCH_PARAMS='';const launch2=await ensureLaunchParams(true);if(!launch2)await tokenSession();return requestApi(action,payload,attempt+1)}if(!r.ok||d?.ok===false)throw new Error(d?.error||('Ошибка сервера '+r.status));return d}catch(e){if(e&&e.name==='AbortError')throw new Error('Сервер не ответил. Проверьте интернет и повторите.');throw e}finally{clearTimeout(timer)}}
  api=(action,payload={})=>requestApi(action,payload,0);
  function showPhoneRegistration(){state.busy=false;$('#content').innerHTML=`<section class="hero"><h2>Регистрация</h2><p class="muted">Введите номер телефона, который владелец указал при добавлении вас в команду.</p><form id="phoneRegForm" class="form"><input name="phone" type="tel" inputmode="tel" autocomplete="tel" placeholder="+7 999 123-45-67" required><button class="primary wide" type="submit">Продолжить</button><p id="phoneRegMsg" class="muted"></p></form></section>`;const form=$('#phoneRegForm');form.onsubmit=async e=>{e.preventDefault();if(state.busy)return;const msg=$('#phoneRegMsg');msg.textContent='Проверяем номер…';setBusy(form,true);try{const d=await requestApi('registerByPhone',{phone:form.elements.phone.value},0);if(d?.session_token)setSession(d.session_token);state.busy=false;await reloadData(false)}catch(err){msg.textContent=err.message;setBusy(form,false)}finally{state.busy=false}}}
  window.BOS_SHOW_PHONE_REGISTRATION=showPhoneRegistration;
  init=async function(){try{$('#content').innerHTML='<section class="hero"><h2>Проверяем доступ…</h2><p class="muted">Вход через ВКонтакте</p></section>';if(!getSession()){let launch=await ensureLaunchParams();if(!launch&&!local){await tokenSession();launch=window.BOS_VK_LAUNCH_PARAMS||''}}await reloadData(false)}catch(e){state.busy=false;if(e?.code==='REGISTRATION_REQUIRED')return showPhoneRegistration();$('#content').innerHTML=`<section class="hero"><h2>Не удалось войти</h2><p class="muted">${esc(e.message)}</p><button class="primary" onclick="init()">Повторить</button></section>`}};
  setTimeout(()=>init(),0);
})();