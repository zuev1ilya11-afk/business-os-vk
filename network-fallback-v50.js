(()=>{
  // Authentication transport is intentionally handled by one auth module now.
  // Keep this file as a no-op compatibility shim while older cached HTML still references it.
  window.BOS_NETWORK_FALLBACK_V50=false;
})();
